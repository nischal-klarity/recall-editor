import React from 'react'
import ReactDOM from 'react-dom/client'
import RecallEditor from '../recall-drawer-v2.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RecallEditor />
  </React.StrictMode>
)
